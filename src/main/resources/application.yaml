spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://keycloak-database:5432/springsecurity
    username: postgres
    password: admin
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
jwt:
  secret: 43iu2h42iy3487fbbsk109nndfh42l
  lifetime: 20m
server:
  port: 2007
